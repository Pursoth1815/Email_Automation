name: thiran_tech
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^5.1.0
  firebase_core: ^3.2.0
  firebase_database: ^11.0.3
  flutter_riverpod: ^2.5.1
  firebase_storage: ^12.1.1
  image_picker: ^1.1.2
  awesome_notifications: ^0.9.3+1
  sqflite: ^2.3.3+1
  dio: ^5.5.0+1
  intl: ^0.19.0
  mailer: ^6.1.1
  path_provider: ^2.1.3
  internet_connection_checker: ^1.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - lib/core/services/data/mail_json_data.json
